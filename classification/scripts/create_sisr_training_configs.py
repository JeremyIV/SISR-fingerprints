# for each classifier that should be trained, creates a training config.
# TODO:
import os
import yaml
from pathlib import path
import argparse

def classifiers_to_train():
	pass # TODO: return list of classifiers to train

def create_config_for(classifier):
	pass # create the config for the given classifier, then save that config to classificaiton/options as a yaml file